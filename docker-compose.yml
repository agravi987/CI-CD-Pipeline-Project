# ==============================================================================
# Root Docker Compose for Local Development
# Allows running 'docker compose up --build' directly from ci-cd-pipeline-project
# ==============================================================================

services:
  # 1. PostgreSQL Database Service
  postgres:
    image: postgres:16-alpine
    container_name: devops_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: devops_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./ci-cd-pipeline-app/db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d devops_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - devops_network

  # 2. Express Backend API Service
  server:
    build:
      context: ./ci-cd-pipeline-app/server
      dockerfile: Dockerfile
    container_name: devops_backend
    restart: unless-stopped
    environment:
      PORT: 5000
      NODE_ENV: development
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgrespassword
      DB_NAME: devops_db
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - devops_network

  # 3. React Frontend Service (Served via Nginx)
  client:
    build:
      context: ./ci-cd-pipeline-app/client
      dockerfile: Dockerfile
    container_name: devops_frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - server
    networks:
      - devops_network

volumes:
  pgdata:

networks:
  devops_network:
    driver: bridge
